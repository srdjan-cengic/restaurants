<%= form_for [:admin, @coupone] do |f| %>
  <% if @coupone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coupone.errors.count, "error") %> prohibited this coupone from being saved:</h2>

      <ul>
      <% @coupone.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :restaurant_id %><br>
    <%= f.collection_select(:restaurant_id, Restaurant.all, :id, :name) %>
  </div>
  <br>

  <div class="field">
    <%= f.label :number_of_available %><br>
    <%= f.number_field :number_of_available, class: "form-control" %>
  </div>
  <br>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description, class: "form-control" %>
  </div>
  <br>
 
  <div class="actions">
    <%= f.submit class: 'btn btn-success' %>
  </div>
<% end %>
